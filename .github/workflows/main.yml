name: '🧬 Release'

on:
  push:
    branches: main

jobs:

  #call-node_modules:
    #uses: .github/workflows/_node_modules.yml@main

  call-build:
    needs: call-test
    uses: .github/workflows/_old_build.yml@main

  call-deploy:
    needs: call-deploy
    uses: .github/workflows/_old_deploy.yml@main