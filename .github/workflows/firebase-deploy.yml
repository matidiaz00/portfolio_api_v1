name: Deploy to Firebase Functions
on:
  push:
    branches:
      - master
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:

      - name: ✅ Checkout branch
        uses: actions/checkout@v2
      
      - name: 🤖 Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 16.14.2

      - name: 🧨 Install NPM dependencies
        run: npm ci

      - name: 💫 Run unit testing
        run: npm run test

      - name: 💥 Build project
        run: npm run build

  Deploy:
    needs: build_project
    runs-on: ubuntu-latest
    steps:

      - name: 🔥Install Firebase tools
        run: npm install -g firebase-tools
      
      - name: 📝 Create firebase.sdk.key.json file
        run: echo '${{ secrets.FIREBASE_SDK_KEY }}' > $HOME/firebase.sdk.key.jsonn

      - name: Assign project to Firebase config
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS=$HOME/firebase.sdk.key.json \
          firebase use ${{ secrets.FIREBASE_PROJECT_ID }}

      - name: 🚀 Deploy to Firebase
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS=$HOME/firebase.sdk.key.json \
          firebase deploy