import { describe, expect, it } from '@jest/globals';

describe('Firebase configuration', () => {

    it(`Initial Firebase Admin in diferents environments`, async () => {
        
    });

    it(`Initial Firebase Client in diferents environments`, async () => {
        
    });

    it(`Connect to admin autentication`, async () => {
        
    });

    it(`Connect to client autentication`, async () => {
        
    });

    it(`Connect to data base (firestore)`, async () => {
        
    });

});