import { describe, expect, it } from '@jest/globals';

describe('Auth', () => {

    describe('Login (model) Middleware', () => {
        it(`Check if request body is correct`, async () => {
            
        });
    });

    describe('Auth Middleware', () => {
        it(`Check if request token exist`, async () => {
            
        });
        it(`Verify if request token is correct`, async () => {
            
        });
        it(`Check if request user is administrator`, async () => {
            
        });
    });

    describe('Login', () => {
        it(`Check if request body is correct`, async () => {
            
        });
        it(`Check if request user exist`, async () => {
            
        });
        it(`Create request user if not exist`, async () => {
            
        });
        it(`Sign in`, async () => {
            
        });
        it(`Get user token from firebase`, async () => {
            
        });
    });

    describe('Logout', () => {
        it(`When is not login`, async () => {
            
        });
        it(`When request user exist`, async () => {
            
        });
    });

    describe('Current User', () => {
        it(`When is not login`, async () => {
            
        });
        it(`Get data of request user`, async () => {
            
        });
    });

});